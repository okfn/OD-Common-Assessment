/* global describe, it */

(function () {
  'use strict';

  describe('Give it some context', function () {
    describe('maybe a bit more context here', function () {
      it('should run here few assertions', function () {

      });
    });
  });
})();


// calculate score correctly

// default indicators visible

// add / remove indicators

// sort by country and score

// group by

    // sort grouping results by predefined order
    // sort grouping results alphabetically
    // maintain sort order of grouped countries
    // resort countries that are grouped

// display only indicators where all visible indicators are shown

// don't display groups where there are no countries visible

// show debug for incorrect data

